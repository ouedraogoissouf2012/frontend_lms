/**
 * Libellés produit (#331).
 *
 * Jamais le mot d'un CRM tiers. Le nom affiché est celui de l'établissement,
 * avec repli neutre. Les composants importent `useBrand`, pas ces chaînes
 * en dur dans le template.
 */
export const BRAND_FALLBACK = 'LMS'
